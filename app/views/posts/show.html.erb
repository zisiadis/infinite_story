
<div id='prompt'>
	<div id='number'><%= Post.count %> entries so far! </div>


<p class = 'blurry-text'><%= Post.find(Post.count-3).line %></p>
<p class = 'blurry-text'><%= Post.find(Post.count-2).line %></p>
<p class = 'blurry-text'><%= Post.find(Post.count-1).line %></p>
<p class = 'blurry-text'><%= Post.find(Post.count).line %></p>
	
		<%= Post.last.line %>...
		</div> 

<div id='mainpage'>
		<div class = 'form'>
<%= form_for(@post) do |f| %>
		

			<div id='line'>
			<%= f.text_field :line %>
			</div>


			<div id='email' >
      		<%=f.label 'Enter your email' %>
      		<%=f.text_field :email, :placeholder => 'to see how the story develops...'%>
  			</div>

	

			<%= f.submit "Add to the Story!", class: 'btn btn-large btn-primary' %>

			<% end %>
		</div>
			</div>
					<div id='counter'><div>

